# 第八天

### 加入redis缓存功能（仅使用公共数据）

    我在lostList，foundList，toplostList中的数据缓存到redis数据库中，
    再次查询的时候会先从redis数据库中查询，如果没有，才会从数据库中查询，然后缓存到redis数据库中
    考虑到更新数据的情况，我在每次数据更新和删除操作都会删除redis数据库中的缓存
    这样只有再次发出查询请求，才会从数据库中查询，更新缓存
    这样避免了如果是改多查少的情况，降低redis数据库更新的频率
    当然这些我只让公共数据进行缓存其他的暂时没有缓存